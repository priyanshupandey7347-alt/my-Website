PORT=4000
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/tamalika?retryWrites=true&w=majority
MONGO_DB_NAME=tamalika

# Set real SMTP values for production email delivery.
# If these are not configured, the backend will use Ethereal test SMTP instead.
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
CONTACT_TO_EMAIL=

ALLOWED_ORIGIN=*
